name: 板块数据每日采集

on:
  workflow_dispatch:
    inputs:
      mode:
        description: '运行模式'
        required: false
        default: 'daily'
        type: choice
        options:
          - daily
          - init
      skip_master:
        description: '跳过板块列表同步（init 模式下 DB 已有数据时使用）'
        required: false
        default: 'false'
        type: choice
        options:
          - 'false'
          - 'true'

jobs:
  collect:
    runs-on: ubuntu-latest
    timeout-minutes: 25

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: 安装依赖
        run: pip install -r scripts/sector-prediction/python/requirements.txt

      - name: 运行采集
        run: |
          if [ "${{ inputs.mode }}" = "init" ]; then
            echo "=== 初始化模式 ==="
            python scripts/sector-prediction/python/init_master.py
          else
            echo "=== 每日增量模式 ==="
            python scripts/sector-prediction/python/main.py
          fi
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_ANON_KEY: ${{ secrets.SUPABASE_ANON_KEY }}
          SKIP_MASTER: ${{ inputs.skip_master }}
